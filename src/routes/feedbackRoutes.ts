import express, { Express, Request, Response } from "express";


const feedbackRoutes: Express = express();
// feedbackRoutes.post("/add", InventoryController.addItem);
// feedbackRoutes.get("/get", InventoryController.getAll);
// feedbackRoutes.put("/updateinventory/:id", InventoryController.updateItem);
// feedbackRoutes.delete("/deleteinventory/:id", InventoryController.deleteItem);
// feedbackRoutes.delete("/setThreshold/:id", InventoryController.threshold);
export default feedbackRoutes;
